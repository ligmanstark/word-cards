//  const searchID = (data: any, searchName = '') => {

// 	const found = data.filter(
// 		(e) => e.'то что ищем'.toLowerCase().includes(searchName.toLowerCase())
// 	);

// 	if (!found) {
// 		const found = data.filter(
// 			(e) => e.'то что ищем'.toLowerCase().includes(searchName.toLowerCase())
// 		);
// 		return found;
// 	} else return found;

// };

// export { searchID };

// //data - указать тип получаемый
// //e - указать тип получаемый
